<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ChecklistTracker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChecklistTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <StackPanel
        VerticalAlignment="Top">
        <MenuBar x:Name="Menu">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Open Layout..." Click="MenuOpenLayout">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator 
                                Modifiers="Control" 
                                Key="O" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutSubItem Text="Layout History" x:Name="LayoutsMenu"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Hide Toolbar" Click="ToggleMenuBar">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator 
                            Key="F11" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Reload" Click="MenuReloadLayout">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator 
                            Modifiers="Control" 
                            Key="R" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>
            <MenuBarItem Title="Checklist Settings">
                <MenuFlyoutItem Text="Load Settings..." Click="MenuLoadSettings"/>
                <MenuFlyoutSubItem Text="Settings History" x:Name="SettingsMenu"/>
            </MenuBarItem>
            <MenuBarItem Title="Screen Capture" x:Name="ScreenCaptureMenu">
                <MenuFlyoutItem Text="Load..." Click="MenuLoadSettings"/>
            </MenuBarItem>
        </MenuBar>
        <Canvas
            x:Name="Layout">

        </Canvas>
    </StackPanel>
</Window>
