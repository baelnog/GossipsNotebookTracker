<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ChecklistTracker.Controls.ElementControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChecklistTracker.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <RelativePanel>
      <Image 
          x:Name="Image"
          Source="{x:Bind CurrentImage, Mode=OneWay }"
          Width="{x:Bind ImageWidth, Mode=OneWay }"
          Height="{x:Bind ImageHeight, Mode=OneWay }"
          Stretch="Uniform"
          VerticalAlignment="Center"
          HorizontalAlignment="Center" />
    <TextBlock 
          x:Name="Counter"
          Text="{x:Bind Count}"
          Visibility="{x:Bind CountVisibility}"
          TextAlignment="End"
          Margin="0,0,0,-8"
          RelativePanel.AlignRightWithPanel="True"
          RelativePanel.AlignBottomWithPanel="True"/>
    </RelativePanel>
</UserControl>
