<?xml version="1.0" encoding="UTF-8"?>
<UserControl
    x:Class="ChecklistTracker.Controls.ScreenCaptureControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodel="using:ChecklistTracker.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel MaxHeight="{x:Bind LayoutParams.Height}" MaxWidth="{x:Bind LayoutParams.Width}">
        <GridView Name="Screenshots" ItemsSource="{x:Bind ViewModel.Screenshots, Mode=OneWay}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="viewmodel:ScreenCapture">
                    <RelativePanel>
                        <Image Source="{x:Bind Screenshot, Mode=OneWay}" Width="{x:Bind LayoutParams.Width}" Height="{x:Bind LayoutParams.Height}"/>
                    </RelativePanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <Button Content="Capture" Click="OnClickCaptureScreenshot">
            <Button.KeyboardAccelerators>
                <KeyboardAccelerator
                    Modifiers="Control"
                    Key="C"/>
            </Button.KeyboardAccelerators>
        </Button>
    </RelativePanel>
</UserControl>