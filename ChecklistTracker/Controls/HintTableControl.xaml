<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ChecklistTracker.Controls.HintTableControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChecklistTracker.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollView 
        x:Name="ScrollLayout"
        Height="{x:Bind TableHeight, Mode=OneWay}"
        VerticalScrollMode="{x:Bind HintScrollMode, Mode=OneWay}">
        <VariableSizedWrapGrid
            x:Name="Layout"
            MaximumRowsOrColumns="{x:Bind MaxColumns, Mode=OneWay}"
            Orientation="Horizontal"
            Background="#404040"
            MinHeight="{x:Bind TableHeight, Mode=OneWay}"
            Width="{x:Bind TableWidth, Mode=OneWay}">
        </VariableSizedWrapGrid>
    </ScrollView>
</UserControl>
