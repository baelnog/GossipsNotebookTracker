<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ChecklistTracker.Controls.SongControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChecklistTracker.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <RelativePanel>
        <Image 
            x:Name="Image"
            Source="{x:Bind ViewModel.CurrentImage, Mode=OneWay }"
            Width="{x:Bind Layout.Width, Mode=OneWay }"
            Height="{x:Bind Layout.Height, Mode=OneWay }"
            Stretch="Uniform"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            CanDrag="False"
            AllowDrop="False" />
        <Image 
            x:Name="Image2"
            Source="{x:Bind ViewModel.BottomImage, Mode=OneWay}"
            Width="{x:Bind BottomImageWidth, Mode=OneWay}"
            Height="{x:Bind BottomImageHeight, Mode=OneWay}"
            Margin="{x:Bind BottomImageMargin, Mode=OneWay}"
            RelativePanel.AlignHorizontalCenterWithPanel="True"
            RelativePanel.AlignBottomWithPanel="True"/>
        <TextBlock
            x:Name="Star"
            Text="★"
            FontSize="16"
            Margin="-6,-10,0,0"
            FontWeight="ExtraBold"
            Foreground="Gold"
            RelativePanel.AlignTopWithPanel="True"
            RelativePanel.AlignLeftWithPanel="True"
            HorizontalTextAlignment="Left"
            VerticalAlignment="Top"
            Visibility="{x:Bind ShowStar, Mode=OneWay}"
            IsTapEnabled="False"
            IsDoubleTapEnabled="False"
            IsHitTestVisible="False"
            IsHoldingEnabled="False"
            IsRightTapEnabled="False"
            />
    </RelativePanel>
</UserControl>
